%h1 Administration

.float-right
  = link_to 'New Scholarship', new_admin_scholarship_path, class: 'btn btn-outline-primary'

%h2 Scholarships

%ul
  - @scholarships.each do |scholarship|
    %li= link_to scholarship, admin_scholarship_path(scholarship)
