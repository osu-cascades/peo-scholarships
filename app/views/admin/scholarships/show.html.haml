%h1 Administration

%h2= @scholarship.name
= link_to 'Edit', edit_admin_scholarship_path(@scholarship), class: 'button'
= link_to 'Delete', admin_scholarship_path(@scholarship), class: 'button', method: :delete, data: {confirm: 'Are you sure you want to delete this scholarship?' }
