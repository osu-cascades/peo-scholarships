wb = xlsx_package.workbook


wb.add_worksheet(name: @scholarship.name + ' Applications') do |sheet|
  sheet.add_row ["Name", "Major"]

  @applications.each do |application|
    sheet.add_row [application.name, application.major]
  end
end