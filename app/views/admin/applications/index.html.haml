%h1 Administration
%h2= @scholarship
%h3 Applications
%table.table.table-hover
  %thead
    %tr
      %th{scope: 'col'} id
      %th{scope: 'col'} Applicant
      %th{scope: 'col'} Status
      %th Last Updated
  %tbody
    - @applications.each do |application|
      %tr
        %th{scope: 'row'}= application.id
        %td= link_to application.applicant, admin_scholarship_application_path(@scholarship, application)
        %td (todo)
        %td= application.updated_at.to_s(:long)
