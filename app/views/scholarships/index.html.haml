%h1 Scholarships
%ul
	- @scholarships.each do |scholarship|
		%li
			= link_to scholarship.name, scholarship
			- if Application.find_by user_id: current_user.id, scholarship_id: scholarship.id
				%p Edit Application
			- else
				= link_to 'New Scholarship', new_scholarship_application_path(scholarship.id), class: 'btn btn-outline-primary'
			